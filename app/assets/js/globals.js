// Globals - with a shared nature

define(function() {
  return {
    HALF_PI: Math.PI / 2,
    TWO_PI: Math.PI * 2,
    E_MINUS_1: Math.E -1,
    mouse: {
      state: 'initial',
      data: { }
    }
  };
});